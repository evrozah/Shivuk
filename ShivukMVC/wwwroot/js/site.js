// Write your JavaScript code.
function changeColorOver(element)
{
    element.classList.add("changed");
    element.classList.remove("prod-tr");
}

function changeColorOut(element) {
    element.classList.add("changed");
}
